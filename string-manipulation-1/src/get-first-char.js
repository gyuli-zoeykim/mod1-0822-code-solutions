/* exported getFirstChar */
// 1. define a function with one parameter string.
// 2. assign a value using array at index to access the first character of string
// 3. return value

function getFirstChar(string) {
  var firstChar = string[0];
  return firstChar;
}
