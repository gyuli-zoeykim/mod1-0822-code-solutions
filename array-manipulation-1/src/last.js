/* exported last */
// 1. define a function with one parameter array
// 2. use array index and length to find last element
// 3. return value

function last(array) {
  var lastElement = array[array.length - 1];
  return lastElement;
}
