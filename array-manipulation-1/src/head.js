/* exported head */
// 1. define a function with one parameter array
// 2. use array index to find first element
// 3. return value

function head(array) {
  var firstElement = array[0];
  return firstElement;
}
